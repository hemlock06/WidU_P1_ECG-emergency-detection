# 홈 PC 원본 복구 및 역사적 대조 결과

**상태: 필수 자산 복구 완료, 역사적 대조 실패로 전수평가 차단.**
앞선 `lead_subset_recovery_report.md`의 자산 미복구 상태는 이 보고서로 갱신한다.
앞선 다운로드·전처리 기록은 당시 실행의 감사 자료로 보존한다.

## 홈 PC 원본 확인과 복사

2026-09-08 18:42 KST, 기존 Drive 작업 큐를 통해 홈 PC `메인컴퓨터`가 응답했다.
`D:/WidU_ecg-fm_emergency-detection_git`에서 기존 test와 참고 ③ 모델을 발견했다.
18:45 원본 해시를 구한 뒤 큐 결과 폴더에 복사하고, 연구실 PC에서 8파일 전부
바이트 수·SHA-256을 대조했다. 전건 일치, 누락 0. 홈 PC 원본은 변경하지 않았다.
`lead_subset_home_asset_transfer.json`에 원본 경로·시각·해시·사본 대조를 보존했다.

- 이진 test: `(474,12,5000)`, 라벨 정상 121·응급 353, 고유 ID 474.
- 5-class test: `(936,12,5000)`, NSR/AF/허혈/전도/이소성 = 130/180/165/379/82,
  이진 라벨 정상 591·응급 345, 고유 ID 936.
- NaN·Inf 0, 라벨·shape·dtype·GPU forward 검사 모두 통과.
- 참고 ③ 모델 SHA-256:
  `ecdb2816fbfecf3ce41ff2e3263e3bdaefd75a5bba81a8e030b7390f03e8d244`.
- P1 test 실행 지문: `c175815089a491399700d9c586c067665dd8b48251196512f77a0b7e2732d91c`.
- 새로 전처리했던 이진 데이터는 `data/processed/cpsc2018_regenerated_20260908`에 보존했다.
  원 `cpsc2018`에는 복원한 test만 배치했다. 새 multi-label 데이터도 별도 보존했다.
- 모델·전체 자산 검사: `lead_subset_home_asset_audit.json`.

## 신규 코드의 로딩 보완

참고 ③은 1,200,146바이트의 adapter-only 체크포인트다. 원 학습 스크립트도
`lora_` 텐서만 저장하고 백본 로딩은 `strict=False`로 수행한다.
새 코드도 모든 LoRA 텐서가 정확히 존재하는지 먼저 검사한 뒤 adapter-only로 로드하도록
수정했다. 전체 백본을 저장하는 P1은 계속 `strict=True`를 사용한다.
누락 adapter 차단·고정 백본 불변 검증을 추가했다. 기존 추적 스크립트는 수정하지 않았다.
최종 테스트 16 passed, 1 warning (4.11초). 변경 코드 Ruff 검사 통과.

## 역사적 대조

환경: RTX 5070 Ti, Python 3.10.21, torch 2.7.1+cu128, CUDA runtime 12.8.
FP32, TF32 off, batch 32, seed 42, 원래 특징 마스킹 동작 유지.
양 모델의 네 리드 조건을 원 코드와 대조해 8/8 통과했다.
P1은 예측 확률 일치, 참고 ③은 기존 AUROC 계산 경로 일치를 검증했다.
이는 역사적 수치 재현과 구분한다.

역사적 숫자가 있는 5개 모델·리드 조건 중 3개 통과, 2개 실패:

| 실패 조건·지표 | 기록값 | 복원 후 실측 | 사전 허용 절대오차 |
|---|---:|---:|---:|
| P1 12리드 Macro-F1 | 0.6858 | 0.69424293 | 0.005 |
| P1 12리드 허혈 AUROC | 0.9066 | 0.90874504 | 0.0021 |
| 참고 ③ I·II Sens@95Sp | 0.7762 | 0.75070822 | 0.02 |

P1 4·2·1리드의 역사적 개별 다중분류 수치는 기록에 없어 `not_available`로 남겼다.
전체 8조건 원시 지표: `exhaustive_lead_subsets_controls.csv`.
기대값·허용오차·모델·데이터·코드 해시: `exhaustive_lead_subsets_controls.json`.
이 manifest의 `status=failed`가 전수 실행을 차단한다.

## 원인 조사: 평가 중 무작위 특징 마스킹

동일한 원본·체크포인트·GPU에서 사전 고정한 6개 진단 조건을 실행했다.
이 진단은 전수 실행 gate를 수정하지 않으며, 통과 seed를 선택하기 위한 탐색이 아니다.

| batch | seed | 특징 마스킹 | P1 Macro-F1 |
|---|---|---|---:|
| 32 | 42 | on | 0.694243 |
| 32 | 43 | on | 0.697188 |
| 32 | 44 | on | 0.688582 |
| 16 | 42 | on | 0.696505 |
| 32 | 42 | off (진단만) | 0.681176 |
| 32 | 43 | off (진단만) | 0.681176 |

mask on에서 seed 변경에 따른 최대 클래스 확률 차이는 0.214803이었다.
mask off에서는 seed 42·43 사이 확률 차이가 정확히 0이었다.
따라서 무작위 특징 마스킹이 평가 변동을 만드는 것은 직접 확인했다.
배치 변경도 특징 마스크의 난수 소비 순서를 바꾼다.
이 실측만으로 과거와 현재 차이 전부가 설명됐다고 단정하지 않는다.

참고 ③의 I·II에서도 batch 32, seed 42/43/44의 Sens@95Sp는 각각
0.750708/0.773371/0.787535였다. 같은 모델·원 test에서 난수만 바꿔 사전 허용오차를
넘는 변동이 관측됐다. 해당 진단은 `lead_subset_control_diagnosis_reference.json`에 있다.

원 학습 코드의 best 체크포인트에는 평가 직전 난수 상태가 없다. 별도 `resume.pt`에는
난수 상태를 저장하는 경로가 있으나 홈 PC의 두 해당 출력 폴더에 이 파일은 없었다.
폴더 실측은 `lead_subset_home_rng_inventory.json`에 보존했다.
마스킹을 끄거나 seed·허용오차를 골라 대조를 통과시킨 것으로 처리하지 않았다.
코드: `scripts/diagnose_lead_subset_controls.py`, 원시 진단: `lead_subset_control_diagnosis.json`.

## 실행 경계

홈 PC의 `outputs/lora_multitask_snr_a07/test_results.npz`도 추가 복구했다.
40,284바이트, 원본·사본 SHA-256
`298e7771660dc25fdc89c3f676954ba123fe2549168148160105e1a18ba6b989` 일치.
저장된 이진·다중 라벨 벡터는 복구한 test와 순서까지 일치하고, 저장 예측에서
다시 계산한 Macro-F1=0.68579700996, 이진 AUROC=0.91389195419,
Sens@95Sp=0.70724637681 및 5개 클래스 AUROC가 문서의 네 자리 값과 전부 일치했다.
따라서 문서의 P1 수치는 실제 보존 예측으로 뒷받침된다.
NPZ에는 record ID가 없으므로 개별 ID-예측 연결을 독립적으로 입증한 것은 아니다.
감사: `lead_subset_historical_prediction_audit.json`.
**저장 예측 재계산 통과를 현재 모델 재추론 양성대조 통과로 대체하지 않는다.**

전수 조합은 P1 0/793, 참고 ③ 0/793. 학습·재학습·전수 실행·push 모두 미실행.
본 회차 역사적 대조 실패 2조건(지표 3개), 구현 대조 실패 0, 전송 실패·스킵 0.
전수 CSV·요약·bootstrap·최적 후보는 아직 없다. 대조 결과를 최적 리드 후보로 부르지 않는다.
주평가는 현재 10초 ECG의 AF·허혈·전도·이소성 분류이며 조기예측 성능이 아니다.

현재 남은 재현 공백은 자산 부재가 아니라 과거 평가의 난수 상태·실행 조건이다.
원래 상태를 더 복원하거나, 별도로 합의한 반복평가·난수 고정 기준을 사전 정의해야 한다.
이번 실패 결과를 보고 seed나 허용오차를 바꿔 전수실행 gate를 통과시키지 않는다.
