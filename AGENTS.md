# 프로젝트 운영 컨텍스트 (이 레포 — 로컬 전용, 커밋 금지)

## 1. 프로젝트 구조 (4 레포 · 병렬 트랙)

- **P1** (`WidU_P1_LoRA-PEFT_Foundation-Model_Adaptation`): ECG-FM + LoRA 심장 검출기. 완료·frozen. cardiac 채널을 다른 레포로 이식.
- **P2-A** (`WidU_P2_Multimodal-Fusion_XAI`): late fusion 접근 — 합성 조립의 구조적 한계 규명 후 보존(아카이브).
- **P2-B** (`WidU_P2_B_Cross_Modal_Attention`): cross-modal attention 융합 — forward 신규 빌드. late fusion 한계를 잇는 후속.
- **시스템/제품** (`WidU_wearable_prototype`): 검증된 검출기 통합(규칙 결합기) + 로깅 + 온디바이스 배포. **결합기·시스템은 여기**.
- **분담**: A=late fusion 전용(결합기는 제품으로 이관). 제품=시스템·배포(det 모델·ONNX/CoreML/TFLite). P2-B=학습 융합.
  학습 융합은 실데이터 확보 후 학습 — 같은 "결합기 슬롯"에 규칙→학습형 교체 가능(동일 인터페이스).
- **단일 진실원천(SSOT)**: `D:\WidU_multimodal_fusion_git\MASTER_DECISION_LOG.md`(로컬·미추적) — 전체 결정·수치·맥락.

## 2. 커밋 작성 규칙 (푸시 전 필터 — "단독 연구" voice)

GitHub는 대학원 지원·외부 공개용. **모든 커밋(메시지 + 커밋되는 문서·코드주석)은 "내가 직접 한 단독 연구"로 읽히도록** 푸시 전 작성·필터링한다.
- **제거**: AI(`Codex`·`Opus`·`Sonnet`·`GPT`·`Anthropic`·어시스턴트) / 협업(`P1·P2 합의`·담당자·`~lane`·`P1·P2 요청`·전할 메시지·통합질문·핸드오프) / 내부프로세스(`MASTER_DECISION_LOG` 참조·`vendored from @<sha>`·커밋 SHA·세션·릴레이) / 상업(`투자자`·`상용 프로토타입`·`필드테스트`→"실세계 검증 연구").
- **유지(정상)**: `vendoring/이식`·`스키마/코드 드리프트`(기술어)·`옵션 A`(CPSC 라벨매핑)·`advisory 모드`·`임상 AI`·`XAI`·모듈명 `P1/P2/P3`.
- **voice**: 학술 논문체(배경·방법·결과·한계·함의). "여정·시행착오·거창함·비표준 조어" 지양.
- 커밋 작성자 = `Jinuk Seok <gbable@naver.com>`. 푸시(force-push 포함)는 **사용자가 직접**. 의심 시 `git grep`으로 전 추적파일 점검.

## 3. 드리프트 가드

- 대화가 길어지면 **목적 재고정**: 현 가용수단으로 최고 수준의 실 웨어러블 시스템 → **30인 실세계 검증 연구**로 "진짜 유효하게 작동"을 입증 → 그 실 성능으로 투자 설득. (대학원 포트폴리오는 후퇴선.)
- **수렴 후 재논쟁 금지** — 실체가 닫히면 추가 flaw-hunting = 드리프트. **"측정으로 닫기"**(의견 대 의견 금지, 실측으로 판정).
- **스코프 freeze** — 비순환적-완전입증·임상급 달성은 목표 아님(데이터 원천 부재). 정직한 한계 명시로 대체.
- **반복 금지**(과거 드리프트 사례): PSG 수집 마라톤, 합성 융합 정교화 경쟁, attention 정확도 선험 우월 가정.
- **인터페이스 동결 계약** — P1→{룰, P2} 출력 계약을 고정하고 변경은 계약 개정으로만. 병렬 세션이 서로 어긋난 걸 빌드하지 않게.

## 4. 세션 간 공유 (COMMS)

**세션 시작 전**: `D:\WidU_multimodal_fusion_git\COMMS.md` 확인.
- **FYI** 항목: 읽고 맥락 파악만. 행동 변경 없음.
- **ACTION/BLOCKER** 항목: **TO에 내 세션명이 있을 때만** 수행. 범위 밖 ACTION 무시.
- 공유할 발견(타 세션에 영향)은 COMMS.md에 항목 추가 후 사용자에게 알릴 것.
- **역할 이탈 방지**: 타 세션 FYI를 읽고 그 세션 일을 대신 시작하면 드리프트. 금지.

> 이 파일은 로컬 전용(`.git/info/exclude`). **절대 커밋·푸시하지 말 것.**
